// ordered collection of object = List [].

void main() {
  List<String> Bruce = ["Banner, Wayne, Springsteen"];
  for (var name in Bruce) {
    print(name);
  }
  for (var element in Bruce) {
    print(element);
  }
}
